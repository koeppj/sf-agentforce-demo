/**
 * @description  Data container for Evidence File details to be used in Flows and Invocable Apex.
 * @author       Generated by Agentforce
 * @date         2026-03-22
 * @group        MyBox
 *
 * This class is designed to be a simple DTO (data transfer object) that can be
 * passed into or returned from Flow actions or Invocable Apex.
 *
 * Security:
 * - This class contains only primitive properties and no behavior.
 * - No DML or SOQL is performed here.
 */
public with sharing class MyBox_EvidenceFile {
    /**
     * Unique identifier for the evidence file (could be ContentVersionId, ContentDocumentId, etc.)
     */
    @InvocableVariable
    public String id { get; set; }

    /**
     * The file name for the evidence (example: image.jpg)
     */
    @InvocableVariable
    public String fileName { get; set; }

    /**
     * High-level category for the evidence (example: DOCUMENT, IMAGE)
     */
    @InvocableVariable
    public String evidenceCategory { get; set; }

    /**
     * Specific type for the evidence (example: RECEIPT, PHOTO)
     */
    @InvocableVariable
    public String evidenceType { get; set; }

    /**
     * Source system or origin of the evidence (example: MOBILE_APP, EMAIL)
     */
    @InvocableVariable
    public String source { get; set; }

    /**
     * Collected date/time in string form (expected ISO-8601 or user-provided format)
     */
    @InvocableVariable
    public String collectedDate { get; set; }

    /**
     * No-arg constructor required for Flow framework object instantiation.
     */
    public MyBox_EvidenceFile() {}

    /**
     * Convenience factory method to build an instance.
     * Note: Not used by @InvocableMethod parameter binding, but useful in Apex code.
     */
    public static MyBox_EvidenceFile of(String id, String fileName, String evidenceCategory, String evidenceType, String source, String collectedDate) {
        MyBox_EvidenceFile dto = new MyBox_EvidenceFile();
        dto.id = id;
        dto.fileName = fileName;
        dto.evidenceCategory = evidenceCategory;
        dto.evidenceType = evidenceType;
        dto.source = source;
        dto.collectedDate = collectedDate;
        return dto;
    }
}